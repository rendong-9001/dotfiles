set -g status-style bg='#0E1116',fg='#DDE6F3'
set -g pane-active-border-style fg='#40E0D0'
set -g window-status-current-format " #[fg=#FFB454,bold]#I:#W "
set -g window-status-activity-style fg='#C678DD'
set -g status-left "#[fg=#40E0D0] #S "
set -g status-right "#[fg=#A0B2C6]%Y-%m-%d %H:%M "

set -g pane-base-index 1
set -g base-index 1
set -g renumber-windows on
set -g history-limit 5000
set -g mouse off

bind-key enter new-window
bind-key h select-pane -L            
bind-key j select-pane -D            
bind-key k select-pane -U            
bind-key l select-pane -R            
bind-key -r C-h resize-pane -L 5     
bind-key -r C-j resize-pane -D 5     
bind-key -r C-k resize-pane -U 5     
bind-key -r C-l resize-pane -R 5     
bind-key c new-window -c "#{pane_current_path}" 
bind-key r source-file ~/.config/tmux/tmux.conf \; display-message "sources tmux.conf"
bind-key x kill-pane                 
bind-key -n C-n next-window
bind-key -n C-p previous-window
unbind-key %
unbind-key '"'
bind-key s split-window
bind-key v split-window -h
bind-key -T copy-mode v send -X begin-selection
bind-key -T copy-mode y send -X copy-selection-and-cancel
