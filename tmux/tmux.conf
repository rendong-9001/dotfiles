set -g status-style bg='#828282',fg='#FFFAFA'
set -g window-status-current-style bg='#DCDCDC',fg='#AEEEEE'
set -g window-status-current-format ' #[fg=#7FFFD4]#I:#W '
set -g window-status-style bg='#2D3036',fg='#868D96'
set -g window-status-activity-style fg='#7FFFD4'
set -g pane-active-border-style fg='#BFEFFF'
set -g pane-border-style fg='#44474E'
set -g status-left "#[fg=#FFFAFA,bg=#3A3F45] #S #[default]"
set -g status-right "#[fg=#FFFAFA,bg=#3A3F45] %Y-%m-%d %H:%M #[default]"

set -g status-position top
set -g default-terminal "xterm-256color"
set -g pane-base-index 1
set -g base-index 1
set -g renumber-windows on
set -g history-limit 6000
set -g mouse off

bind-key enter new-window
bind-key h select-pane -L            
bind-key j select-pane -D            
bind-key k select-pane -U            
bind-key l select-pane -R            
bind-key -r C-h resize-pane -L 5     
bind-key -r C-j resize-pane -D 5     
bind-key -r C-k resize-pane -U 5     
bind-key -r C-l resize-pane -R 5     
bind-key c new-window -c "#{pane_current_path}" 
bind-key r source-file ~/.config/tmux/tmux.conf \; display-message "sources tmux.conf"
bind-key x kill-pane                 
bind-key -n C-n next-window
bind-key -n C-p previous-window
unbind-key %
unbind-key '"'
bind-key s split-window
bind-key v split-window -h
bind-key -T copy-mode v send -X begin-selection
bind-key -T copy-mode y send -X copy-selection-and-cancel
