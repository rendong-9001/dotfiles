set -g window-status-activity-style 'fg=yellow,bright'
set -g window-status-current-style 'bg=black,fg=green,bright'
set -g window-status-current-format '#I:#W'
set -g window-status-style 'bg=black,fg=blue,bright'
set -g window-status-separator "#[fg=white,bg=black]┆"
set -g pane-border-lines simple
set -g pane-border-style 'fg=black'
set -g pane-active-border-style 'fg=cyan,bright'
set -g status-style 'bg=default'
set -g status-right '#[fg=white,bg=black]#(cat /tmp/weather/result) | %Y-%m-%d %H:%M'
set -g status-left '#[fg=white]#S '

set -g status-interval 60
set -g status-position top
set -g default-terminal "screen-256color"
set -ga terminal-features ",xterm-256color:RGB"
set -g pane-base-index 1
set -g base-index 1
set -g renumber-windows on
set -g history-limit 8192
set -g mouse off
set -g set-clipboard on
set -g default-command "${SHELL}"
set -g status-right-length 50

bind-key enter new-window
bind-key h select-pane -L
bind-key j select-pane -D
bind-key k select-pane -U
bind-key l select-pane -R
bind-key -r C-h resize-pane -L 5
bind-key -r C-j resize-pane -D 5
bind-key -r C-k resize-pane -U 5
bind-key -r C-l resize-pane -R 5
bind-key c new-window -c "#{pane_current_path}" 
bind-key r source-file ~/.config/tmux/tmux.conf \; display-message 'sources tmux.conf'
bind-key x kill-pane                 
bind-key -n C-n next-window
bind-key -n C-p previous-window
unbind-key %
unbind-key '"'
bind-key - split-window -v -c "#{pane_current_path}"
bind-key | split-window -h -c "#{pane_current_path}"
bind-key s split-window -v -c "#{pane_current_path}"
bind-key v split-window -h -c "#{pane_current_path}"
bind-key -T copy-mode v send -X begin-selection
bind-key -T copy-mode y send -X copy-selection-and-cancel
bind-key o confirm-before -p "Kill all other panes? (y/n)" 'kill-pane -a'
bind-key g popup -w 60% -h 60% -d "#{pane_current_path}"
bind-key f popup -w 60% -h 60% -d "#{pane_current_path}" "nnn -e"
unbind C-b
set -g prefix `
bind ` send-prefix
